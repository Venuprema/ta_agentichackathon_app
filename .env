# Copy to .env and set your values. .env is in the project root and is loaded by the app and src/llm.py, src/data_loaders.py.

# Required to run the workflow
GEMINI_API_KEY=sk-InetzCyPbJIEEsRTCLvZvA

# AI Gateway base URL (e.g. LiteLLM). When set, app uses OpenAI-compatible client.
GEMINI_BASE_URL=https://api.ai-gateway.tigeranalytics.com

# Optional: model name for gateway (default: gemini-1.5-flash)
GEMINI_MODEL=gemini-2.5-flash

# Optional: override data directory for CSVs (default: project_root/data)
# WENDYS_DATA_DIR=./data
